# 租租侠项目底座实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付「租租侠」的可运行工程底座 —— 前后端骨架跑通、接口文档冻结、数据库建表脚本可用，使前端 2 人、后端 2 人能够完全并行开工。

**Architecture:** 单仓库双目录（`frontend/` + `backend/`）。前端 Vue 3 + Vite 运行于 :5173，通过 Vite proxy 把 `/api/**` 转发到后端 :8080；后端 Spring Boot 3 分层为 Controller → Service → Mapper → MySQL 8.0，用 JWT 拦截器做认证，所有响应走统一结构 `{code, message, data}`。

**Tech Stack:** Vue 3 · Vite · Element Plus · Pinia · Vue Router · axios ｜ Java 21 · Spring Boot 3 · MyBatis-Plus · jjwt · MySQL 8.0 ｜ Maven Wrapper

**关联设计文档:** `docs/specs/2026-09-17-zuzuxia-底座设计.md`

---

## 环境前置条件（执行前必读）

经实测，开发机当前状态如下：

| 组件 | 状态 | 处置 |
|---|---|---|
| Node.js v22.19.0 | ✅ 可用 | — |
| npm 10.9.3 | ✅ 可用 | — |
| JDK 21.0.7 (Oracle) `D:\code tool\java-jdk` | ✅ 可用，`JAVA_HOME` 已设置 | — |
| **Maven** | ❌ **未安装** | **本计划全程使用 Maven Wrapper `mvnw.cmd`，无需安装 Maven** |
| **MySQL 8.0 服务 `MySQL80`** | ⚠️ **Stopped 且 Disabled** | 见 Task 0 |
| start.spring.io | ✅ 可达 | 用于生成后端工程骨架 |

**网络注意**：Maven Central 在国内可能较慢。若 `mvnw` 下载依赖超时，见 Task 6 的阿里云镜像配置。

---

## Task 0: 启动并启用 MySQL 服务

**Files:** 无（环境操作）

- [ ] **Step 1: 将 MySQL80 服务改为自动启动并拉起**

```powershell
Set-Service -Name MySQL80 -StartupType Automatic
Start-Service -Name MySQL80
Get-Service MySQL80 | Select-Object Name, Status, StartType
```

预期输出：`Status = Running`，`StartType = Automatic`

- [ ] **Step 2: 验证能连上 MySQL**

```powershell
& 'C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe' -u root -p -e "SELECT VERSION();"
```

预期输出：`8.0.x`

> 若忘记 root 密码，在 `C:\ProgramData\MySQL\MySQL Server 8.0\my.ini` 的 `[mysqld]` 段临时加 `skip-grant-tables` 后重启服务重置，重置完务必删除该行。

- [ ] **Step 3: 记录密码**

后续 `application-dev.yml` 与 `scripts/init-db.bat` 都要用。**不要提交到 Git**（`.gitignore` 已排除 `application-local.yml`）。

---

## Task 1: 仓库基础设施

**Files:**
- Create: `.gitignore`
- Modify: `README.md`（整体重写）

- [ ] **Step 1: 创建 `.gitignore`**

```gitignore
# ---- Java / Maven ----
target/
*.class
*.jar
*.war
!.mvn/wrapper/maven-wrapper.jar

# ---- Node / Vue ----
node_modules/
dist/
dist-ssr/
*.local
.vite/

# ---- IDE ----
.idea/
*.iml
*.ipr
*.iws
.vscode/
!.vscode/extensions.json

# ---- OS ----
Thumbs.db
Desktop.ini
.DS_Store

# ---- 项目私有 ----
uploads/
application-local.yml
*.log
logs/
```

- [ ] **Step 2: 重写 `README.md`**

内容需包含：项目简介、技术栈表、目录结构、环境要求（JDK 21 / Node 22 / MySQL 8.0）、启动步骤（引用 `scripts/`）、分支约定、**三条全局约定**（`/api` 前缀、统一响应体、openapi.yaml 为唯一依据）、成员分工表。

- [ ] **Step 3: 提交**

```bash
git add .gitignore README.md
git commit -m "chore: 添加 .gitignore 并完善 README"
```

---

## Task 2: 数据库建表脚本

**Files:**
- Create: `docs/db/schema.sql`

- [ ] **Step 1: 编写 `schema.sql`**

必须包含建库语句与 9 张表，字符集统一 `utf8mb4` / `utf8mb4_general_ci`，引擎 `InnoDB`。

```sql
DROP DATABASE IF EXISTS zuzuxia;
CREATE DATABASE zuzuxia DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
USE zuzuxia;

-- 用户表
CREATE TABLE `user` (
  `id`             BIGINT       NOT NULL AUTO_INCREMENT COMMENT '主键',
  `username`       VARCHAR(50)  NOT NULL COMMENT '登录名',
  `password`       VARCHAR(100) NOT NULL COMMENT 'BCrypt 加密',
  `nickname`       VARCHAR(50)  DEFAULT NULL COMMENT '昵称',
  `phone`          VARCHAR(20)  DEFAULT NULL COMMENT '手机号',
  `avatar`         VARCHAR(255) DEFAULT NULL COMMENT '头像 URL',
  `role`           VARCHAR(20)  NOT NULL DEFAULT 'USER' COMMENT 'USER/ADMIN',
  `status`         VARCHAR(20)  NOT NULL DEFAULT 'NORMAL' COMMENT 'NORMAL/BANNED',
  `credit_score`   INT          NOT NULL DEFAULT 100 COMMENT '信用分',
  `balance`        DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '可用余额',
  `frozen_balance` DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '冻结余额',
  `created_at`     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at`     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户';

-- 分类表
CREATE TABLE `category` (
  `id`     BIGINT      NOT NULL AUTO_INCREMENT,
  `name`   VARCHAR(50) NOT NULL COMMENT '分类名',
  `sort`   INT         NOT NULL DEFAULT 0 COMMENT '排序',
  `icon`   VARCHAR(255) DEFAULT NULL,
  `status` VARCHAR(20) NOT NULL DEFAULT 'ENABLED' COMMENT 'ENABLED/DISABLED',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_name` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='物品分类';

-- 物品表
CREATE TABLE `item` (
  `id`             BIGINT        NOT NULL AUTO_INCREMENT,
  `owner_id`       BIGINT        NOT NULL COMMENT '出租人',
  `category_id`    BIGINT        NOT NULL,
  `title`          VARCHAR(100)  NOT NULL,
  `description`    TEXT,
  `location`       VARCHAR(100)  DEFAULT NULL COMMENT '取件地点',
  `daily_price`    DECIMAL(10,2) NOT NULL COMMENT '日租金',
  `deposit`        DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '押金',
  `cover_image`    VARCHAR(255)  DEFAULT NULL,
  `status`         VARCHAR(20)   NOT NULL DEFAULT 'ON_SHELF' COMMENT 'ON_SHELF/OFF_SHELF/DELISTED',
  `view_count`     INT           NOT NULL DEFAULT 0,
  `favorite_count` INT           NOT NULL DEFAULT 0,
  `created_at`     DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at`     DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_owner`    (`owner_id`),
  KEY `idx_category` (`category_id`),
  KEY `idx_status`   (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='物品';

-- 物品图片表
CREATE TABLE `item_image` (
  `id`      BIGINT       NOT NULL AUTO_INCREMENT,
  `item_id` BIGINT       NOT NULL,
  `url`     VARCHAR(255) NOT NULL,
  `sort`    INT          NOT NULL DEFAULT 0,
  PRIMARY KEY (`id`),
  KEY `idx_item` (`item_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='物品图片';

-- 可租时间段表
CREATE TABLE `item_available_period` (
  `id`         BIGINT NOT NULL AUTO_INCREMENT,
  `item_id`    BIGINT NOT NULL,
  `start_date` DATE   NOT NULL,
  `end_date`   DATE   NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_item_date` (`item_id`, `start_date`, `end_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='可租时间段';

-- 订单表（order 为保留字，故命名 orders）
CREATE TABLE `orders` (
  `id`             BIGINT        NOT NULL AUTO_INCREMENT,
  `order_no`       VARCHAR(32)   NOT NULL COMMENT '业务单号',
  `item_id`        BIGINT        NOT NULL,
  `owner_id`       BIGINT        NOT NULL COMMENT '冗余，避免联表 item',
  `renter_id`      BIGINT        NOT NULL,
  `start_date`     DATE          NOT NULL,
  `end_date`       DATE          NOT NULL,
  `days`           INT           NOT NULL,
  `daily_price`    DECIMAL(10,2) NOT NULL COMMENT '下单时快照',
  `rent_amount`    DECIMAL(10,2) NOT NULL COMMENT '租金小计',
  `deposit_amount` DECIMAL(10,2) NOT NULL COMMENT '押金快照',
  `total_amount`   DECIMAL(10,2) NOT NULL,
  `status`         VARCHAR(20)   NOT NULL DEFAULT 'PENDING'
                   COMMENT 'PENDING/REJECTED/CANCELLED/RESERVED/RENTING/RETURNING/FINISHED',
  `apply_remark`   VARCHAR(255)  DEFAULT NULL,
  `reject_reason`  VARCHAR(255)  DEFAULT NULL,
  `created_at`     DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `confirmed_at`   DATETIME      DEFAULT NULL,
  `picked_at`      DATETIME      DEFAULT NULL,
  `returned_at`    DATETIME      DEFAULT NULL,
  `finished_at`    DATETIME      DEFAULT NULL,
  `cancelled_at`   DATETIME      DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_item_period` (`item_id`, `start_date`, `end_date`, `status`),
  KEY `idx_renter` (`renter_id`),
  KEY `idx_owner`  (`owner_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单';

-- 钱包流水表
CREATE TABLE `wallet_transaction` (
  `id`            BIGINT        NOT NULL AUTO_INCREMENT,
  `user_id`       BIGINT        NOT NULL,
  `type`          VARCHAR(30)   NOT NULL
                  COMMENT 'RECHARGE/DEPOSIT_FREEZE/DEPOSIT_UNFREEZE/DEPOSIT_DEDUCT/RENT_PAY/RENT_INCOME/DEPOSIT_REFUND',
  `amount`        DECIMAL(10,2) NOT NULL COMMENT '正数收入，负数支出',
  `balance_after` DECIMAL(10,2) NOT NULL COMMENT '变动后可用余额',
  `order_id`      BIGINT        DEFAULT NULL,
  `remark`        VARCHAR(255)  DEFAULT NULL,
  `created_at`    DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_time` (`user_id`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='钱包流水';

-- 评价表
CREATE TABLE `review` (
  `id`          BIGINT      NOT NULL AUTO_INCREMENT,
  `order_id`    BIGINT      NOT NULL,
  `reviewer_id` BIGINT      NOT NULL,
  `reviewee_id` BIGINT      NOT NULL,
  `role`        VARCHAR(20) NOT NULL COMMENT 'LENDER/RENTER',
  `score`       TINYINT     NOT NULL COMMENT '1-5',
  `content`     VARCHAR(500) DEFAULT NULL,
  `created_at`  DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_reviewer` (`order_id`, `reviewer_id`),
  KEY `idx_reviewee` (`reviewee_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='双向评价';

-- 收藏表
CREATE TABLE `favorite` (
  `id`         BIGINT   NOT NULL AUTO_INCREMENT,
  `user_id`    BIGINT   NOT NULL,
  `item_id`    BIGINT   NOT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_item` (`user_id`, `item_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='收藏';
```

- [ ] **Step 2: 执行脚本验证无语法错误**

```powershell
& 'C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe' -u root -p < docs\db\schema.sql
```

- [ ] **Step 3: 验证 9 张表都建出来了**

```powershell
& 'C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe' -u root -p -e "USE zuzuxia; SHOW TABLES;"
```

预期输出恰好 9 行：`category` `favorite` `item` `item_available_period` `item_image` `orders` `review` `user` `wallet_transaction`

- [ ] **Step 4: 提交**

```bash
git add docs/db/schema.sql
git commit -m "feat(db): 添加 9 张表的建表脚本"
```

---

## Task 3: 测试数据脚本

**Files:**
- Create: `docs/db/data.sql`

- [ ] **Step 1: 编写 `data.sql`**

要求：
- 1 个管理员（`admin` / `admin123`）、5 个普通用户，密码均为 BCrypt 值
- 每个普通用户 `balance = 1000.00`
- 8 个分类：摄影器材、影音设备、户外露营、电动工具、家用电器、数码配件、乐器、运动健身
- 至少 20 件物品，覆盖全部分类，`daily_price` 5–200 元、`deposit` 50–2000 元
- 物品的可租时间段统一设为 `2026-09-01` 至 `2026-12-31`
- 覆盖全部 7 种订单状态，每种至少 1 单
- 若干条评价与收藏，使信用档案、热度榜有数据可展示

> BCrypt 密文可用 `admin123` 预生成，**不要**在脚本里调用 Java 方法。

- [ ] **Step 2: 导入并验证**

```powershell
& 'C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe' -u root -p zuzuxia < docs\db\data.sql
& 'C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe' -u root -p -e "USE zuzuxia; SELECT status, COUNT(*) FROM orders GROUP BY status;"
```

预期输出 7 行状态统计。

- [ ] **Step 3: 提交**

```bash
git add docs/db/data.sql
git commit -m "feat(db): 添加演示用测试数据"
```

---

## Task 4: ER 说明文档

**Files:**
- Create: `docs/db/er.md`

- [ ] **Step 1: 编写 ER 说明**

含 Mermaid `erDiagram`（GitHub 原生渲染）+ 每张表的一句话职责说明 + 关键外键关系 + 索引设计理由。

- [ ] **Step 2: 提交**

```bash
git add docs/db/er.md
git commit -m "docs(db): 添加 ER 图与表职责说明"
```

---

## Task 5: 接口文档 openapi.yaml

**Files:**
- Create: `docs/api/openapi.yaml`

- [ ] **Step 1: 编写 OpenAPI 3.0 规范**

覆盖设计文档 8.5 节全部约 45 个接口。结构要求：

```yaml
openapi: 3.0.3
info:
  title: 租租侠租赁平台 API
  version: 1.0.0
servers:
  - url: http://localhost:8080/api
paths:
  /auth/login:
    post:
      tags: [认证]
      summary: 用户登录
      # ...
components:
  schemas:
    Result:          # 统一响应体
      type: object
      properties:
        code:    { type: integer, example: 200 }
        message: { type: string,  example: success }
        data:    { nullable: true }
    PageResult:
      type: object
      properties:
        total: { type: integer }
        list:  { type: array, items: {} }
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
security:
  - bearerAuth: []
```

必填内容：
- 每个接口的 `summary`、请求参数（query/path/body）、全部响应码（含 401/403 与业务码）
- 所有 DTO/VO 的字段定义与类型
- 全局 `security` 标记，公开接口单独标 `security: []`

- [ ] **Step 2: 语法校验**

```powershell
npx --yes @redocly/cli lint docs/api/openapi.yaml
```

预期：无 error（warning 可接受）

- [ ] **Step 3: 提交**

```bash
git add docs/api/openapi.yaml
git commit -m "docs(api): 添加 OpenAPI 接口规范（唯一对接依据）"
```

---

## Task 6: 后端工程生成与依赖配置

**Files:**
- Create: `backend/`（由 Spring Initializr 生成）
- Modify: `backend/pom.xml`

- [ ] **Step 1: 用 Spring Initializr 生成工程骨架（含 Maven Wrapper）**

```powershell
cd D:\deepseekharness\zuzuxia
curl.exe -sS -o backend.zip "https://start.spring.io/starter.zip?type=maven-project&language=java&bootVersion=3.3.5&groupId=com.zuzuxia&artifactId=zuzuxia-backend&name=zuzuxia-backend&packageName=com.zuzuxia&javaVersion=21&dependencies=web,mysql,validation,lombok"
Expand-Archive backend.zip -DestinationPath backend-tmp -Force
Move-Item backend-tmp\zuzuxia-backend backend
Remove-Item backend.zip, backend-tmp -Recurse -Force
Get-ChildItem backend
```

预期：出现 `pom.xml`、`mvnw`、`mvnw.cmd`、`.mvn/`、`src/`

- [ ] **Step 2: 在 `pom.xml` 中补加依赖**

在 `<dependencies>` 内添加：

```xml
<!-- MyBatis-Plus -->
<dependency>
  <groupId>com.baomidou</groupId>
  <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
  <version>3.5.7</version>
</dependency>

<!-- JWT -->
<dependency>
  <groupId>io.jsonwebtoken</groupId>
  <artifactId>jjwt-api</artifactId>
  <version>0.12.6</version>
</dependency>
<dependency>
  <groupId>io.jsonwebtoken</groupId>
  <artifactId>jjwt-impl</artifactId>
  <version>0.12.6</version>
  <scope>runtime</scope>
</dependency>
<dependency>
  <groupId>io.jsonwebtoken</groupId>
  <artifactId>jjwt-jackson</artifactId>
  <version>0.12.6</version>
  <scope>runtime</scope>
</dependency>

<!-- BCrypt（仅需 crypto，无需引入完整 Security） -->
<dependency>
  <groupId>org.springframework.security</groupId>
  <artifactId>spring-security-crypto</artifactId>
</dependency>

<!-- Knife4j 接口文档 UI -->
<dependency>
  <groupId>com.github.xiaoymin</groupId>
  <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
  <version>4.5.0</version>
</dependency>
```

- [ ] **Step 3: 配置阿里云 Maven 镜像（可选，加速依赖下载）**

在 `backend/.mvn/` 下新建 `settings.xml` 不起作用（wrapper 不读它）。改为在 `pom.xml` 中加仓库：

```xml
<repositories>
  <repository>
    <id>aliyun</id>
    <url>https://maven.aliyun.com/repository/public</url>
    <releases><enabled>true</enabled></releases>
    <snapshots><enabled>false</enabled></snapshots>
  </repository>
</repositories>
```

- [ ] **Step 4: 验证 Maven Wrapper 可用（首次会下载依赖，耗时较长）**

```powershell
cd backend
.\mvnw.cmd -q clean compile
```

预期：`BUILD SUCCESS`

- [ ] **Step 5: 提交**

```bash
git add backend/
git commit -m "feat(backend): 生成 Spring Boot 3 工程骨架并配置依赖"
```

---

## Task 7: 后端配置文件

**Files:**
- Create: `backend/src/main/resources/application.yml`
- Create: `backend/src/main/resources/application-dev.yml`
- Create: `backend/src/main/resources/application-local.yml.example`
- Delete: `backend/src/main/resources/application.properties`

- [ ] **Step 1: 编写 `application.yml`**

```yaml
server:
  port: 8080
spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: null
  mapper-locations: classpath*:mapper/**/*.xml
jwt:
  secret: zuzuxia-secret-key-for-course-project-please-change-in-production-2026
  expire-hours: 24
upload:
  path: ./uploads/
  url-prefix: /uploads/
```

- [ ] **Step 2: 编写 `application-dev.yml`**

```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zuzuxia?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 请填写Task0记录的密码
```

- [ ] **Step 3: 编写 `application-local.yml.example`** 作为密码模板，并在 README 说明用法

- [ ] **Step 4: 验证启动**

```powershell
cd backend
.\mvnw.cmd spring-boot:run
```

预期：日志出现 `Tomcat started on port 8080`，且无数据库连接异常。

- [ ] **Step 5: 提交**

```bash
git add backend/src/main/resources/
git commit -m "feat(backend): 添加 application 配置"
```

---

## Task 8: 公共层（统一响应体与异常处理）

**Files:**
- Create: `backend/src/main/java/com/zuzuxia/common/Result.java`
- Create: `backend/src/main/java/com/zuzuxia/common/ResultCode.java`
- Create: `backend/src/main/java/com/zuzuxia/common/BizException.java`
- Create: `backend/src/main/java/com/zuzuxia/common/GlobalExceptionHandler.java`
- Test: `backend/src/test/java/com/zuzuxia/common/ResultTest.java`

- [ ] **Step 1: 编写失败测试 `ResultTest.java`**

```java
package com.zuzuxia.common;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ResultTest {

    @Test
    void success_should_carry_code_200_and_data() {
        Result<String> r = Result.success("hello");
        assertEquals(200, r.getCode());
        assertEquals("success", r.getMessage());
        assertEquals("hello", r.getData());
    }

    @Test
    void error_should_carry_business_code_and_message() {
        Result<Void> r = Result.error(ResultCode.BALANCE_NOT_ENOUGH);
        assertEquals(1003, r.getCode());
        assertEquals("余额不足", r.getMessage());
        assertNull(r.getData());
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```powershell
cd backend
.\mvnw.cmd -q test -Dtest=ResultTest
```

预期：编译失败，`Result` / `ResultCode` 不存在

- [ ] **Step 3: 实现 `ResultCode.java`**

按设计文档 8.4 节，用枚举承载全部 13 个码：

```java
package com.zuzuxia.common;

import lombok.Getter;

@Getter
public enum ResultCode {
    SUCCESS(200, "success"),
    PARAM_ERROR(400, "参数校验失败"),
    UNAUTHORIZED(401, "未登录或登录已过期"),
    FORBIDDEN(403, "无权限访问"),
    NOT_FOUND(404, "资源不存在"),
    CONFLICT(409, "操作冲突"),
    SYSTEM_ERROR(500, "系统异常，请稍后重试"),

    USERNAME_EXISTS(1001, "用户名已存在"),
    LOGIN_FAILED(1002, "用户名或密码错误"),
    BALANCE_NOT_ENOUGH(1003, "余额不足"),
    PERIOD_OCCUPIED(1004, "该时段已被租用"),
    ORDER_STATUS_INVALID(1005, "当前订单状态不允许此操作"),
    RENT_OWN_ITEM(1006, "请勿租借自己的物品"),
    ORDER_NOT_FINISHED(1007, "订单未完成，无法评价"),
    ITEM_OFF_SHELF(1008, "物品已下架");

    private final int code;
    private final String message;

    ResultCode(int code, String message) {
        this.code = code;
        this.message = message;
    }
}
```

- [ ] **Step 4: 实现 `Result.java`**

```java
package com.zuzuxia.common;

import lombok.Data;

@Data
public class Result<T> {
    private int code;
    private String message;
    private T data;

    public static <T> Result<T> success(T data) {
        Result<T> r = new Result<>();
        r.code = ResultCode.SUCCESS.getCode();
        r.message = ResultCode.SUCCESS.getMessage();
        r.data = data;
        return r;
    }

    public static <T> Result<T> success() {
        return success(null);
    }

    public static <T> Result<T> error(ResultCode rc) {
        Result<T> r = new Result<>();
        r.code = rc.getCode();
        r.message = rc.getMessage();
        return r;
    }

    public static <T> Result<T> error(ResultCode rc, String message) {
        Result<T> r = error(rc);
        r.message = message;
        return r;
    }
}
```

- [ ] **Step 5: 实现 `BizException.java`**

```java
package com.zuzuxia.common;

import lombok.Getter;

@Getter
public class BizException extends RuntimeException {
    private final ResultCode resultCode;

    public BizException(ResultCode resultCode) {
        super(resultCode.getMessage());
        this.resultCode = resultCode;
    }

    public BizException(ResultCode resultCode, String message) {
        super(message);
        this.resultCode = resultCode;
    }
}
```

- [ ] **Step 6: 实现 `GlobalExceptionHandler.java`**

```java
package com.zuzuxia.common;

import lombok.extern.slf4j.Slf4j;
import org.springframework.validation.FieldError;
import org.springframework.web.bind.MethodArgumentNotValidException;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

@Slf4j
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(BizException.class)
    public Result<Void> handleBiz(BizException e) {
        return Result.error(e.getResultCode(), e.getMessage());
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<Void> handleValid(MethodArgumentNotValidException e) {
        FieldError fe = e.getBindingResult().getFieldError();
        String msg = fe == null ? ResultCode.PARAM_ERROR.getMessage() : fe.getDefaultMessage();
        return Result.error(ResultCode.PARAM_ERROR, msg);
    }

    @ExceptionHandler(Exception.class)
    public Result<Void> handleOther(Exception e) {
        log.error("系统异常", e);
        return Result.error(ResultCode.SYSTEM_ERROR);
    }
}
```

- [ ] **Step 7: 跑测试确认通过**

```powershell
.\mvnw.cmd -q test -Dtest=ResultTest
```

预期：`BUILD SUCCESS`，2 个测试通过

- [ ] **Step 8: 提交**

```bash
git add backend/src/main/java/com/zuzuxia/common/ backend/src/test/
git commit -m "feat(backend): 添加统一响应体、错误码枚举与全局异常处理"
```

---

## Task 9: MyBatis-Plus 与 Web 配置

**Files:**
- Create: `backend/src/main/java/com/zuzuxia/config/MybatisPlusConfig.java`
- Create: `backend/src/main/java/com/zuzuxia/config/WebMvcConfig.java`
- Modify: `backend/src/main/java/com/zuzuxia/ZuzuxiaApplication.java`

- [ ] **Step 1: 添加分页插件与 Mapper 扫描**

`MybatisPlusConfig.java` 注册 `MybatisPlusInterceptor` + `PaginationInnerInterceptor(DbType.MYSQL)`；`ZuzuxiaApplication` 上加 `@MapperScan("com.zuzuxia.mapper")`。

- [ ] **Step 2: 配置 CORS 与静态资源映射**

`WebMvcConfig.java` 实现 `WebMvcConfigurer`：
- `addCorsMappings`：允许 `http://localhost:5173`，放行全部方法与头
- `addResourceHandlers`：把 `/uploads/**` 映射到 `upload.path` 指向的本地目录（`file:./uploads/`），使上传的图片可直接通过 URL 访问

- [ ] **Step 3: 验证编译通过**

```powershell
cd backend
.\mvnw.cmd -q clean compile
```

- [ ] **Step 4: 提交**

```bash
git add backend/src/main/java/com/zuzuxia/config/ backend/src/main/java/com/zuzuxia/ZuzuxiaApplication.java
git commit -m "feat(backend): 配置 MyBatis-Plus 分页、CORS 与静态资源映射"
```

---

## Task 10: JWT 认证

**Files:**
- Create: `backend/src/main/java/com/zuzuxia/security/JwtUtil.java`
- Create: `backend/src/main/java/com/zuzuxia/security/UserContext.java`
- Create: `backend/src/main/java/com/zuzuxia/security/AuthInterceptor.java`
- Create: `backend/src/main/java/com/zuzuxia/security/AdminInterceptor.java`
- Modify: `backend/src/main/java/com/zuzuxia/config/WebMvcConfig.java`
- Test: `backend/src/test/java/com/zuzuxia/security/JwtUtilTest.java`

- [ ] **Step 1: 编写失败测试 `JwtUtilTest.java`**

```java
package com.zuzuxia.security;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class JwtUtilTest {
    private final JwtUtil jwtUtil = new JwtUtil(
        "zuzuxia-secret-key-for-course-project-please-change-in-production-2026", 24);

    @Test
    void generate_then_parse_should_return_same_user() {
        String token = jwtUtil.generate(1L, "alice", "USER");
        assertNotNull(token);
        assertEquals(1L, jwtUtil.getUserId(token));
        assertEquals("alice", jwtUtil.getUsername(token));
        assertEquals("USER", jwtUtil.getRole(token));
    }

    @Test
    void parse_tampered_token_should_return_null() {
        assertNull(jwtUtil.getUserId("not.a.valid.token"));
    }
}
```

- [ ] **Step 2: 运行确认失败**

```powershell
cd backend
.\mvnw.cmd -q test -Dtest=JwtUtilTest
```

预期：编译失败，`JwtUtil` 不存在

- [ ] **Step 3: 实现 `JwtUtil.java`**

用 jjwt 0.12.x API（`Jwts.builder()` / `Jwts.parser().verifyWith(key).build()`），payload 放 `userId`、`username`、`role`；解析异常一律 `return null`，由调用方转 401。

- [ ] **Step 4: 实现 `UserContext.java`**

基于 `ThreadLocal<Long>` 保存当前登录用户 id 与角色，提供 `set/get/clear`。

- [ ] **Step 5: 实现 `AuthInterceptor.java` 与 `AdminInterceptor.java`**

- `AuthInterceptor`：从 `Authorization: Bearer <token>` 取 token → 解析 → 存入 `UserContext`；失败则响应 `401` + `Result.error(UNAUTHORIZED)` 的 JSON，并 `return false`
- `AdminInterceptor`：校验 `UserContext` 中角色为 `ADMIN`，否则 `403`
- 两者 `afterCompletion` 中必须 `UserContext.clear()`，防止线程池串号

- [ ] **Step 6: 在 `WebMvcConfig` 注册拦截器**

```java
registry.addInterceptor(authInterceptor)
        .addPathPatterns("/api/**")
        .excludePathPatterns(
            "/api/auth/login",
            "/api/auth/register",
            "/api/categories",
            "/api/items", "/api/items/*", "/api/items/hot", "/api/items/recommend",
            "/api/items/*/available-periods"
        );

registry.addInterceptor(adminInterceptor)
        .addPathPatterns("/api/admin/**");
```

> **注意**：排除列表必须与设计文档 8.3 节完全一致。`/api/items/mine` **不在排除列表**，因为它需要登录。

- [ ] **Step 7: 跑测试确认通过**

```powershell
.\mvnw.cmd -q test -Dtest=JwtUtilTest
```

预期：`BUILD SUCCESS`

- [ ] **Step 8: 提交**

```bash
git add backend/src/main/java/com/zuzuxia/security/ backend/src/test/
git commit -m "feat(backend): 实现 JWT 认证与权限拦截"
```

---

## Task 11: 连通性接口与端到端验证

**Files:**
- Create: `backend/src/main/java/com/zuzuxia/controller/PingController.java`
- Test: `backend/src/test/java/com/zuzuxia/controller/PingControllerTest.java`

- [ ] **Step 1: 编写失败测试**

用 `@WebMvcTest(PingController.class)` + `MockMvc`，断言 `GET /api/ping` 返回 `code=200` 且 `data` 含 `"zuzuxia"`。

- [ ] **Step 2: 运行确认失败**

```powershell
.\mvnw.cmd -q test -Dtest=PingControllerTest
```

- [ ] **Step 3: 实现 `PingController.java`**

```java
@RestController
@RequestMapping("/api")
public class PingController {
    @GetMapping("/ping")
    public Result<Map<String, Object>> ping() {
        Map<String, Object> data = new LinkedHashMap<>();
        data.put("app", "zuzuxia");
        data.put("time", LocalDateTime.now().toString());
        return Result.success(data);
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

```powershell
.\mvnw.cmd -q test -Dtest=PingControllerTest
```

- [ ] **Step 5: 真机验证**

```powershell
cd backend
.\mvnw.cmd spring-boot:run
```

另开一个终端：

```powershell
curl.exe -sS http://localhost:8080/api/ping
```

预期：`{"code":200,"message":"success","data":{"app":"zuzuxia","time":"..."}}`

- [ ] **Step 6: 提交**

```bash
git add backend/src/main/java/com/zuzuxia/controller/ backend/src/test/
git commit -m "feat(backend): 添加连通性接口 /api/ping"
```

---

## Task 12: 前端工程生成

**Files:**
- Create: `frontend/`（由 Vite 脚手架生成）

- [ ] **Step 1: 生成 Vue 3 工程**

```powershell
cd D:\deepseekharness\zuzuxia
npm create vite@latest frontend -- --template vue
cd frontend
npm install
```

- [ ] **Step 2: 安装依赖**

```powershell
npm install element-plus @element-plus/icons-vue pinia vue-router axios
```

- [ ] **Step 3: 清理脚手架默认内容**

删除 `src/components/HelloWorld.vue`、`src/assets/vue.svg`、`src/style.css` 中的默认样式，保留最小 `App.vue`。

- [ ] **Step 4: 验证能启动**

```powershell
npm run dev
```

预期：`Local: http://localhost:5173/`

- [ ] **Step 5: 提交**

```bash
git add frontend/
git commit -m "feat(frontend): 生成 Vue 3 + Vite 工程并安装依赖"
```

---

## Task 13: 前端 axios 封装与 API 模块

**Files:**
- Create: `frontend/src/api/request.js`
- Create: `frontend/src/api/auth.js`
- Create: `frontend/src/api/item.js`
- Create: `frontend/src/api/order.js`
- Create: `frontend/src/api/wallet.js`
- Create: `frontend/src/api/review.js`
- Create: `frontend/src/api/favorite.js`
- Create: `frontend/src/api/admin.js`
- Modify: `frontend/vite.config.js`

- [ ] **Step 1: 配置 Vite proxy**

```js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { fileURLToPath, URL } from 'node:url'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: { '@': fileURLToPath(new URL('./src', import.meta.url)) }
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

- [ ] **Step 2: 编写 `request.js`**

```js
import axios from 'axios'
import { ElMessage } from 'element-plus'
import router from '@/router'

const request = axios.create({
  baseURL: '/api',
  timeout: 15000
})

// 请求拦截器：统一带 token
request.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token')
    if (token) config.headers.Authorization = `Bearer ${token}`
    return config
  },
  (error) => Promise.reject(error)
)

// 响应拦截器：统一判 code
request.interceptors.response.use(
  (response) => {
    const res = response.data
    if (res.code === 200) return res.data

    if (res.code === 401) {
      localStorage.removeItem('token')
      localStorage.removeItem('userInfo')
      ElMessage.error('登录已过期，请重新登录')
      router.push('/login')
      return Promise.reject(new Error(res.message))
    }

    ElMessage.error(res.message || '操作失败')
    return Promise.reject(new Error(res.message))
  },
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      router.push('/login')
    }
    ElMessage.error(error.message || '网络异常')
    return Promise.reject(error)
  }
)

export default request
```

- [ ] **Step 3: 按其模块编写 API 函数**

每个模块文件导出与 `openapi.yaml` 一一对应的函数，例如：

```js
// src/api/auth.js
import request from './request'

export const login    = (data) => request.post('/auth/login', data)
export const register = (data) => request.post('/auth/register', data)
export const getMe    = ()     => request.get('/auth/me')
```

其余模块依此类推，函数名与路径必须与 `openapi.yaml` 严格一致。

- [ ] **Step 4: 提交**

```bash
git add frontend/src/api/ frontend/vite.config.js
git commit -m "feat(frontend): 封装 axios 与各模块 API"
```

---

## Task 14: 前端路由、布局与状态管理

**Files:**
- Create: `frontend/src/router/index.js`
- Create: `frontend/src/stores/user.js`
- Create: `frontend/src/layouts/UserLayout.vue`
- Create: `frontend/src/layouts/AdminLayout.vue`
- Create: `frontend/src/views/Home.vue`（连通性验证页）
- Create: `frontend/src/views/Login.vue`（占位）
- Modify: `frontend/src/main.js`
- Modify: `frontend/src/App.vue`

- [ ] **Step 1: 编写 `stores/user.js`**

Pinia store，state 含 `token`、`userInfo`；actions 含 `login()`、`logout()`、`restore()`（从 localStorage 恢复）；getter `isAdmin`。

- [ ] **Step 2: 编写路由**

```js
const routes = [
  { path: '/login', component: () => import('@/views/Login.vue') },
  {
    path: '/',
    component: () => import('@/layouts/UserLayout.vue'),
    children: [
      { path: '', name: 'home', component: () => import('@/views/Home.vue') }
      // 物品大厅、详情、发布、订单、钱包、信用档案、个人中心（后续按分工补充）
    ]
  },
  {
    path: '/admin',
    component: () => import('@/layouts/AdminLayout.vue'),
    meta: { role: 'ADMIN' },
    children: [
      // 用户管理、物品管理、订单管理、分类管理（后续补充）
    ]
  }
]
```

- [ ] **Step 3: 编写全局前置守卫**

```js
router.beforeEach((to) => {
  const token = localStorage.getItem('token')
  if (to.path !== '/login' && !token) return '/login'
  if (to.meta.role === 'ADMIN') {
    const info = JSON.parse(localStorage.getItem('userInfo') || '{}')
    if (info.role !== 'ADMIN') return '/'
  }
})
```

- [ ] **Step 4: 编写两套布局**

- `UserLayout.vue`：顶部导航（Logo、大厅、发布、订单、钱包、头像下拉）+ `<router-view />`
- `AdminLayout.vue`：`el-container` + 左侧 `el-menu` 管理菜单 + 右侧内容区

- [ ] **Step 5: 编写 `Home.vue` 连通性验证页**

页面加载时调用 `request.get('/ping')`，把返回的 `app` 与 `time` 渲染出来，并显示"后端连通成功 ✅"。

- [ ] **Step 6: 在 `main.js` 注册 Element Plus、Pinia、Router**

- [ ] **Step 7: 端到端验证**

同时启动前后端后，浏览器访问 `http://localhost:5173`，应看到"后端连通成功 ✅"。

- [ ] **Step 8: 提交**

```bash
git add frontend/src/
git commit -m "feat(frontend): 添加路由、双布局与状态管理，打通端到端"
```

---

## Task 15: 一键启动脚本

**Files:**
- Create: `scripts/init-db.bat`
- Create: `scripts/start-all.bat`
- Create: `scripts/stop-all.bat`

- [ ] **Step 1: 编写 `init-db.bat`**

依次执行 `schema.sql` 与 `data.sql`；用 `set /p` 交互读取 MySQL 密码，不硬编码。

- [ ] **Step 2: 编写 `start-all.bat`**

```bat
@echo off
chcp 65001 >nul
echo [1/3] 检查 MySQL 服务...
net start MySQL80 >nul 2>&1

echo [2/3] 启动后端 (8080)...
start "zuzuxia-backend" cmd /k "cd /d %~dp0..\backend && mvnw.cmd spring-boot:run"

echo [3/3] 启动前端 (5173)...
start "zuzuxia-frontend" cmd /k "cd /d %~dp0..\frontend && npm run dev"

echo 启动完成，等待 30 秒后访问 http://localhost:5173
```

- [ ] **Step 3: 编写 `stop-all.bat`** 关闭两个窗口并停止相关 java/node 进程

- [ ] **Step 4: 实测一键启动**

关闭所有相关窗口后双击 `scripts/start-all.bat`，确认前后端都起来且页面能连通。

- [ ] **Step 5: 提交**

```bash
git add scripts/
git commit -m "chore: 添加一键启动与初始化脚本"
```

---

## Task 16: 边界测试用例文档

**Files:**
- Create: `docs/test-cases.md`

- [ ] **Step 1: 编写 6 个边界用例**

按设计文档 10 节，每个用例含：编号、前置条件、操作步骤、预期结果（含预期错误码）。六个用例：

| 编号 | 场景 | 预期 |
|---|---|---|
| TC-01 | 在不可租时段提交申请 | 1004 该时段已被租用 |
| TC-02 | 对同一物品同一时段重复申请 | 1004 |
| TC-03 | 租借自己发布的物品 | 1006 请勿租借自己的物品 |
| TC-04 | 余额不足以支付租金+押金 | 1003 余额不足 |
| TC-05 | 两人同时对同一单点"同意" | 仅一人成功，另一人 1004 |
| TC-06 | 对同一订单重复评价 | 409 操作冲突（由数据库唯一索引 `uk_order_reviewer` 兜底拦截） |

- [ ] **Step 2: 提交**

```bash
git add docs/test-cases.md
git commit -m "docs: 添加 6 个边界测试用例"
```

---

## Task 17: 收尾验证

- [ ] **Step 1: 全新克隆验证**

```powershell
cd D:\
git clone https://github.com/romarin-star/zuzuxia.git zuzuxia-verify
cd zuzuxia-verify\backend
.\mvnw.cmd -q clean package -DskipTests
cd ..\frontend
npm install
```

预期：均无错误。

- [ ] **Step 2: 按 README 从零走一遍**，确认新成员能照文档独立跑通

- [ ] **Step 3: 清理验证目录**

```powershell
Remove-Item D:\zuzuxia-verify -Recurse -Force
```

- [ ] **Step 4: 推送全部分支**

```bash
git push origin main
```

---

## 完成标准（Definition of Done）

1. `git clone` 全新副本后，照 README 能独立启动前后端
2. 浏览器访问 `http://localhost:5173` 显示"后端连通成功 ✅"
3. MySQL 中 9 张表建成，测试数据覆盖全部 7 种订单状态
4. `docs/api/openapi.yaml` 通过 lint 校验，覆盖约 45 个接口
5. 6 个边界测试用例已记录成文
6. `scripts/start-all.bat` 双击即可拉起全套环境
